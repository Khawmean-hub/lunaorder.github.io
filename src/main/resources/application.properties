
jwt.secret=luna
spring.jpa.hibernate.ddl-auto=update
jwt.http.request.header=Authorization
# jwt.token.expiration.in.seconds=604800
jwt.token.expiration.in.seconds=120

spring.jpa.show-sql=true

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=200MB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
file.upload-dir=${user.dir}/src/main/resources/files/
file.upload.client.path=/images/**
file.upload.server.path=${user.dir}/src/main/resources/files/


app.firebase-configuration-file=piggy-c33de-firebase-adminsdk-i0kq1-247811104e.json